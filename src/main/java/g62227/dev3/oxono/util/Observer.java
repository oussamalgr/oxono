package g62227.dev3.oxono.util;

/**
 *   Represents an observer in the Observer design pattern.
 */
public interface Observer {
    /**
     * Called to notify the observer of a change or event.
     */
    void update();
}
